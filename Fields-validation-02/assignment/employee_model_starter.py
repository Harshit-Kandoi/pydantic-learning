from pydantic import BaseModel, Field

# TODO: Create a Employee Model
# Fields:
# - id: int (primary key, auto increment)
# - name: str (min 3 chars, max 50 chars))
# - department: optional str (default 'General')
# - salary: float (must be >= 10000)